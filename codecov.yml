coverage:
  status:
    project:
      default:
        target: auto
        threshold: 1%
    patch:
      default:
        target: 90%
        threshold: 2%

# Exclude files that are either trivial entrypoints or contain
# branches gated by GOOS that CI cannot exercise.
ignore:
  - "main.go"
